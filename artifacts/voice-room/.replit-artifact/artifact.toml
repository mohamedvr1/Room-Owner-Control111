kind = "web"
previewPath = "/"
title = "Voice Room"
version = "1.0.0"
id = "artifacts/voice-room"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21551

[services.development]
run = "pnpm --filter @workspace/voice-room run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/voice-room", "run", "build" ]
publicDir = "artifacts/voice-room/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21551"
BASE_PATH = "/"
